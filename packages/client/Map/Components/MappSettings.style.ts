import { makeStyles } from '@material-ui/core';

export default makeStyles({
  icon: {
    position: 'absolute',
    top: '1em',
    right: '1em',
    color: 'black',
    zIndex: 10000,
    cursor: 'pointer',
  },
});
