import { makeStyles } from '@material-ui/styles';

export default makeStyles({
  main: {
    display: 'flex',
    marginBottom: '.3em',
  },

  entry: {
    display: 'flex',
    flexDirection: 'column',
    marginRight: '.5em',
    alignItems: 'center',
  },
});
