import { makeStyles } from '@material-ui/styles';
export default makeStyles({
  wrapper: {
    position: 'relative',
    margin: '20px',
  },
  loading: {
    position: 'absolute',
    top: '-0.8em',
    right: '1.1em',
    transform: 'scale(.5)',
  },
});
