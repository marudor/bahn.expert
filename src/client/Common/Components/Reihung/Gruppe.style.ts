import { makeStyles } from '@material-ui/styles';

export default makeStyles({
  bezeichnung: {
    display: 'flex',
    flexDirection: 'column',
    position: 'absolute',
    bottom: '2.5em',
    textAlign: 'center',
  },
  br: {
    marginRight: '.5em',
  },
});
