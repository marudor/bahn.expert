import { makeStyles, MergedTheme } from '@material-ui/styles';

export default makeStyles<MergedTheme>({
  main: {
    position: 'absolute',
    fontWeight: 'bolder',
    textAlign: 'center',
  },
});
