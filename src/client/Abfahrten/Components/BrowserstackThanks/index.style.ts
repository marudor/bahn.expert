import { makeStyles } from '@material-ui/styles';

export default makeStyles({
  main: {
    marginTop: '2em',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-between',
  },
});
