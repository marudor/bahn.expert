import { makeStyles } from '@material-ui/core';

export default makeStyles({
  map: {
    flex: 1,
  },
  settings: {
    position: 'absolute',
    top: '1em',
    right: '1em',
    color: 'black',
    zIndex: 1000,
  },
});
