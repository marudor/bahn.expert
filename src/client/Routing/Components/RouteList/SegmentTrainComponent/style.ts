import { makeStyles } from '@material-ui/styles';

export default makeStyles({
  trainInfo: {
    display: 'flex',
    alignItems: 'center',
  },
  trainMargin: {
    marginRight: '.5em',
  },
  destination: {
    flex: 1,
    textAlign: 'center',
  },
  reihung: {
    fontSize: '0.5em',
  },
});
